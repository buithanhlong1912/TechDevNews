import React, { ReactElement } from 'react'

interface Props {
    
}

export default function AdminProfile({  }: Props): ReactElement {
    return (
        <div>
            profile here
        </div>
    )
}
